# Optional: MySQL initialization scripts
# Place .sql or .sh files here to run on first container startup
# Example: 01-init-db.sql, 02-seed-data.sql

# These scripts run automatically when the container is first created
# and the data volume is empty
